Real app screenshots for the marketing site's Features page + Home showcase.

Capture these 5 from the seeded demo salon (musenail.github.io/turndesk/?salon=demo,
front-desk PIN 1111) and save them here with EXACTLY these names:

  turngrid.png   — Floor → Turns sub-tab (the tech rows with today's tickets)
  floorplan.png  — Floor → Floor Plan sub-tab (stations + tech avatars)
  checkin.png    — the walk-in check-in form
  reports.png    — Reports tab → Payroll (or the daily report)
  payments.png   — a ticket's Pay / checkout screen

Framing doesn't need to be exact — the site crops each into a browser-window frame
via CSS (see .appshot in ../site.css; per-image object-position is set in
features.html / index.html). Full-window captures are fine.

PNG or JPG both work; if you save as .jpg, tell me and I'll switch the <img> paths.
